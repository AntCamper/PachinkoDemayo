import React from 'react';

function GameBoard({ balls }) {
  return (
    <div className="game-board">
      {/* We'll add the game board elements here */}
    </div>
  );
}

export default GameBoard;